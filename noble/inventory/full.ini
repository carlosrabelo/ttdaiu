# Full environment inventory
# For complete local installations with all features enabled

[local]
localhost ansible_connection=local

[full:children]
local

[full:vars]
# Full environment settings - all features enabled
backup_enabled=false
debug_mode=true
apt_upgrade_type=dist
log_level=debug