---
- name: Install @openai/codex (latest)
  become: true
  ansible.builtin.command: npm install -g @openai/codex@latest
  environment:
    PATH: "/snap/bin:{{ ansible_env.PATH }}"
  register: codex_install
  changed_when: codex_install.rc == 0
  when: not ansible_check_mode
